# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(rock_tutorialTaskLib)
ADD_LIBRARY(${ROCK_TUTORIAL_TASKLIB_NAME} SHARED 
    ${ROCK_TUTORIAL_TASKLIB_SOURCES})

add_dependencies(${ROCK_TUTORIAL_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${ROCK_TUTORIAL_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${ROCK_TUTORIAL_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${ROCK_TUTORIAL_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${ROCK_TUTORIAL_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${ROCK_TUTORIAL_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${ROCK_TUTORIAL_TASKLIB_HEADERS}
    DESTINATION include/orocos/rock_tutorial)

